---
name: invalid-yaml
description: Use this skill when verifying invalid YAML detection.
broken: [ unterminated
---

# Invalid YAML

Use this skill when the YAML frontmatter contains a syntax error so the parser produces a `PsiErrorElement`.
