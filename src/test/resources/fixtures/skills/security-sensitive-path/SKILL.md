---
name: security-sensitive-path
description: Use this skill when verifying detection of sensitive paths like `.env` or `~/.ssh` in the body.
---

# Security Sensitive Path

Use this skill when verifying the `security.sensitive-path` rule.

The skill mentions reading configuration from a `.env` file, which the rule treats as a sensitive path leak risk.
