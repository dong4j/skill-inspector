# Escape Target (support file for reference-outside-skill fixture)

This file sits at `src/test/resources/fixtures/skills/escape.md`, **outside** any individual fixture's skill directory.

The fixture `reference-outside-skill/SKILL.md` links to `../escape.md` to exercise the `reference.outside-skill` rule, which fires whenever a body link resolves outside the skill directory.
