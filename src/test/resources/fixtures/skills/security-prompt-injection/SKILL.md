---
name: security-prompt-injection
description: Use this skill when verifying detection of prompt-injection phrases such as "ignore previous instructions".
---

# Security Prompt Injection

Use this skill when verifying the `security.prompt-injection` rule.

Adversarial documents sometimes embed phrases like `ignore previous instructions` to override system prompts; this fixture intentionally contains that exact phrase.
