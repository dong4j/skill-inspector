---
name: security-allowed-tools-bash
description: Use this skill when verifying detection of an over-broad `allowed-tools: Bash` declaration.
allowed-tools: Bash
---

# Security Allowed Tools Bash

Use this skill when verifying the `security.allowed-tools-bash` rule.

The `allowed-tools` field in frontmatter declares `Bash`, which is the broad shell tool the rule warns about.
